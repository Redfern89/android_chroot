# rootfs_mounter

Скрипт для монтирования и запуска chroot-окружений (RootFS) на Android-устройствах (через терминал) или Debian-системах. Автоматизирует проброс системных директорий, работу с loop-устройствами и обход специфических ограничений Android-ядра.

## 🚀 Возможности
*   **Авто-loop:** Сам находит свободное loop-устройство и цепляет твой образ.
*   **Fix Permissions:** Перемонтирует `/data` с правами `suid` (решает проблемы с sudo внутри chroot).
*   **Kernel Check:** Проверяет поддержку Namespaces, Loop-девайсов и статус SELinux.
*   **Bind Mounts:** Пробрасывает `/dev`, `/sys`, `/proc`, а также внешние SD-карты и внутреннюю память.
*   **Android-специфика:** Маскирует Android-биндеры (`binder`, `hwbinder`), чтобы не конфликтовали с системными сервисами внутри образа.
*   **Cleanup:** После выхода из chroot автоматически убивает зависшие процессы и размонтирует все точки.

## 🛠 Требования
*   Устройство с Root-правами (Magisk / APatch / KernelSU).
*   Установленный Busybox (желательно).
*   Образ RootFS в формате `.img` или `.ext4`.

## 📂 Использование

1. Дай права на исполнение:
   ```bash
   chmod +x mount_rootfs.sh